// Tailwind CSS v4 for Next.js uses the dedicated PostCSS plugin
// (the Vite build previously used @tailwindcss/vite).
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
