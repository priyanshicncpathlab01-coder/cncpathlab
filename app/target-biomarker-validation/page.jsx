import TargetBiomarkerValidation from '@/views/TargetBiomarkerValidation';

// Static content -> SSG (App Router default). Regenerated only at build time.
export const metadata = {
  title: 'Target & Biomarker Validation',
  description: 'Target identification and biomarker validation services to de-risk drug development programs.',
  alternates: { canonical: '/target-biomarker-validation' },
};

export default function Page() {
  return <TargetBiomarkerValidation />;
}
